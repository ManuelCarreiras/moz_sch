# Liceu Santa Isabel Frontend

## Development

- Run with Docker Compose:
  ```sh
  docker-compose up --build
  ```
- The frontend will be available at http://localhost:5173

## Manual (if you have Node.js):
  ```sh
  cd frontend
  npm install
  npm run dev -- --host
  ``` 